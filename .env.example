# Optional: Waitlist form POST URL. When set, the "Join Waitlist" form will POST { "email": "..." } to this endpoint.
# Leave empty to show a local success message without calling a backend.
# VITE_WAITLIST_API_URL=

# Optional: "Watch Demo" button URL (e.g. YouTube). When set, the button opens this URL in a new tab.
# VITE_DEMO_VIDEO_URL=
